# T001-T003 测试运行指南

## 🚀 快速运行方法

### ⚠️ 重要：先安装依赖

在运行测试之前，需要先安装测试依赖：

```bash
cd ~/ai-stack-super-enhanced
cd super_agent_main_interface

# 方法1: 使用安装脚本（推荐）
./安装测试依赖.sh

# 方法2: 手动安装
python3 -m pip install pytest pytest-asyncio httpx --user

# 方法3: 使用requirements.txt
python3 -m pip install -r requirements.txt --user
```

### 运行测试

#### 方法1: 使用符号链接（推荐）

```bash
cd ~/ai-stack-super-enhanced
cd super_agent_main_interface
./scripts/run_workflow_validation_tests.sh
```

#### 方法2: 使用完整路径

```bash
cd ~/ai-stack-super-enhanced
cd "🚀 Super Agent Main Interface"
./scripts/run_workflow_validation_tests.sh
```

#### 方法3: 使用项目根目录的脚本

```bash
cd ~/ai-stack-super-enhanced
./运行工作流验证测试.sh
```

#### 方法4: 在主界面目录直接运行

```bash
cd ~/ai-stack-super-enhanced
cd "🚀 Super Agent Main Interface"
./运行测试.sh
```

#### 方法5: 使用Python直接运行（最可靠）

```bash
cd ~/ai-stack-super-enhanced
cd super_agent_main_interface

# T001: 工作流集成测试
python3 -m pytest tests/test_workflow_integration.py -v

# T002: RAG双检索测试
python3 -m pytest tests/test_rag_double_retrieval.py -v

# T003: 2秒SLO测试
python3 -m pytest tests/performance/test_slo_2s.py -v
```

## 📝 常见问题解决

### 问题1: 脚本注释错误

**错误**: `command not found` (三引号注释)

**已修复**: 所有脚本的注释已改为 `#` 格式

### 问题2: pytest未安装

**错误**: `No module named pytest`

**解决方案**:
```bash
# 使用--user标志安装（推荐）
python3 -m pip install pytest pytest-asyncio httpx --user

# 或使用安装脚本
cd super_agent_main_interface
./安装测试依赖.sh
```

### 问题3: 目录路径问题

**错误**: `cd: no such file or directory`

**解决方案**:
```bash
# 使用符号链接（推荐）
cd ~/ai-stack-super-enhanced/super_agent_main_interface

# 或使用完整路径（用引号）
cd ~/ai-stack-super-enhanced/"🚀 Super Agent Main Interface"
```

### 问题4: 权限错误

**错误**: `Permission denied`

**解决方案**:
```bash
chmod +x "🚀 Super Agent Main Interface/scripts/run_workflow_validation_tests.sh"
chmod +x "🚀 Super Agent Main Interface/运行测试.sh"
chmod +x "🚀 Super Agent Main Interface/安装测试依赖.sh"
```

## 🔍 查看测试报告

测试报告会自动保存到 `logs/workflow/` 目录：

```bash
# 查看最新报告
ls -lt super_agent_main_interface/logs/workflow/*.json | head -3

# 查看报告内容
cat super_agent_main_interface/logs/workflow/integration_test_report_*.json | python3 -m json.tool
```

## 📊 测试结果说明

- **T001测试**: 验证工作流完整链路，检查所有步骤可追踪性
- **T002测试**: 验证RAG双检索功能，检查两次检索的输入输出
- **T003测试**: 验证API性能，检查2秒SLO合规率

所有测试都会生成JSON格式的详细报告，即使部分失败也会提供完整的测试结果。

## 🛠️ 完整安装步骤

```bash
# 1. 进入目录
cd ~/ai-stack-super-enhanced
cd super_agent_main_interface

# 2. 安装依赖
python3 -m pip install pytest pytest-asyncio httpx --user

# 3. 验证安装
python3 -m pytest --version

# 4. 运行测试
./scripts/run_workflow_validation_tests.sh
```

## 💡 使用虚拟环境（可选）

如果遇到系统包管理冲突，可以使用虚拟环境：

```bash
cd ~/ai-stack-super-enhanced/super_agent_main_interface

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 运行测试
python3 -m pytest tests/test_workflow_integration.py -v
python3 -m pytest tests/test_rag_double_retrieval.py -v
python3 -m pytest tests/performance/test_slo_2s.py -v

# 退出虚拟环境
deactivate
```
