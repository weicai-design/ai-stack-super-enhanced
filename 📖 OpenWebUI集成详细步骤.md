# 📖 OpenWebUI集成AI Stack详细步骤

## 🎯 目标
在OpenWebUI聊天框中输入问题，自动触发AI Stack各功能模块，实现智能对话交互。

---

## 📋 准备工作

### 1. 确认服务运行状态

```bash
# 检查OpenWebUI
curl http://localhost:3000/health

# 检查AI Stack服务
curl http://localhost:9000/gateway/status
```

**必须确保**：
- ✅ OpenWebUI运行中（端口3000）
- ✅ AI Stack各服务运行中（RAG 8011, ERP 8013等）

---

## 🔧 详细安装步骤

### 第1步：打开OpenWebUI

在浏览器访问：**http://localhost:3000**

### 第2步：登录/注册

- 如果是首次使用，需要先注册账号
- **重要**：第一个注册的账号自动成为管理员
- 管理员账号才能安装Functions

### 第3步：进入Functions管理页面

**方法1：通过菜单**
```
1. 点击左下角的用户头像/用户名
2. 在弹出菜单中选择 "Settings"（设置）
3. 在左侧菜单中找到 "Workspace"
4. 点击 "Functions"
```

**方法2：直接访问URL**
```
http://localhost:3000/workspace/functions
```

### 第4步：创建新Function

在Functions页面：
1. 点击右上角的 **"+" 按钮**
2. 出现代码编辑器界面

### 第5步：粘贴Plugin代码

1. **代码已在剪贴板中**（运行了复制脚本）
2. 在代码编辑器中 **Command+V 粘贴**
3. 你会看到一大段Python代码（约500行）
4. 点击底部的 **"Save"** 按钮

### 第6步：配置Plugin

保存后，在Functions列表中会出现新的Function：
```
AI Stack Intelligent Master Plugin
```

点击这个Function右侧的 **⚙️ 图标**（设置）

### 第7步：设置API地址

在配置页面，找到并设置以下参数：

```json
{
  "rag_api": "http://host.docker.internal:8011",
  "erp_api": "http://host.docker.internal:8013",
  "stock_api": "http://host.docker.internal:8014",
  "content_api": "http://host.docker.internal:8016",
  "learning_api": "http://host.docker.internal:8019"
}
```

**为什么用 `host.docker.internal`？**
- OpenWebUI运行在Docker容器中
- `host.docker.internal` 是Docker访问宿主机的特殊域名
- 这样容器才能访问你Mac上运行的AI Stack服务

### 第8步：启用所有功能开关

在同一配置页面，找到以下开关，**全部设为 true**：

```json
{
  "enable_auto_rag": true,
  "enable_smart_routing": true,
  "enable_interaction_learning": true,
  "enable_expert_analysis": true,
  "enable_auto_rag_ingest": true,
  "enable_self_evolution": true
}
```

**各开关功能**：
- `enable_auto_rag`: 自动RAG知识检索
- `enable_smart_routing`: 智能路由（自动识别问题类型）
- `enable_interaction_learning`: 交互学习（记录对话）
- `enable_expert_analysis`: 专家分析建议
- `enable_auto_rag_ingest`: 对话自动入库RAG
- `enable_self_evolution`: 自我进化（每100次对话优化）

点击 **"Save"** 保存配置。

### 第9步：启用Plugin

确保Plugin的**主开关是绿色**（开启状态）：

在Functions列表中，每个Function左侧有个开关：
- 🟢 绿色 = 已启用
- ⚪ 灰色 = 已禁用

如果是灰色，点击开关变成绿色。

### 第10步：在聊天中启用Function

创建新聊天或进入现有聊天：

1. 点击聊天输入框上方的 **"Functions"** 图标
2. 在Function列表中，确保 **"AI Stack Intelligent Master Plugin"** 被勾选
3. 现在这个聊天就会使用AI Stack功能了

---

## 🧪 测试验证

### 测试1：RAG知识检索

在OpenWebUI聊天框输入：
```
什么是机器学习？
```

**成功的表现**：
```
🧠 AI Stack智能分析中...
✅ 已集成RAG知识+实时数据+专家分析

【📚 知识库检索】
1. 机器学习是人工智能的一个分支...

【👨‍🔬 专家分析建议】
💡 建议：建议结合实际案例理解...

[AI详细回答]
```

**失败的表现**：
- 只有普通AI回答
- 没有"AI Stack智能分析中..."提示
- 没有知识库检索和专家建议

### 测试2：ERP自动查询

输入：
```
今天的财务数据
```

**成功的表现**：
```
🎯 识别到ERP相关问题，正在查询系统...

【📊 ERP系统实时数据】
收入: ¥5,000,000
支出: ¥3,000,000
利润: ¥2,000,000

【👨‍🔬 财务专家建议】
💡 建议：关注收支平衡...

[AI专业分析]
```

### 测试3：股票查询

输入：
```
贵州茅台的股价
```

**成功的表现**：
```
🎯 识别到股票相关问题...

【📊 股票系统实时数据】
贵州茅台 (600519)
当前价格: ¥1,850.00

【👨‍🔬 投资专家建议】
💡 建议：注意风险控制...
```

### 测试4：查看学习进度

在终端执行：
```bash
curl http://localhost:8019/api/learning/stats
```

如果返回学习统计数据，说明自我学习系统在工作。

---

## ❓ 常见问题解决

### 问题1：找不到Functions菜单

**原因**：
- 未登录
- 不是管理员账号
- OpenWebUI版本太旧

**解决**：
1. 确保已登录
2. 使用第一个注册的账号（管理员）
3. 直接访问：http://localhost:3000/workspace/functions
4. 如果还不行，重启OpenWebUI：
   ```bash
   docker restart open-webui
   ```

### 问题2：Plugin保存后无效果

**原因**：
- API地址配置错误
- 功能开关未启用
- Plugin主开关未开启
- AI Stack服务未运行

**解决**：
1. 检查配置中的API地址是否为 `host.docker.internal`
2. 确认所有6个功能开关都是 `true`
3. 确认Plugin主开关是绿色
4. 验证AI Stack服务运行：
   ```bash
   curl http://localhost:9000/gateway/status
   ```

### 问题3：报错 "Connection refused"

**原因**：AI Stack服务未运行

**解决**：
```bash
cd /Users/ywc/ai-stack-super-enhanced
./scripts/start_all_final.sh
```

### 问题4：只有普通AI回答

**原因**：
- Plugin未在聊天中启用
- Function选择器中未勾选

**解决**：
1. 在聊天输入框上方点击 "Functions" 图标
2. 勾选 "AI Stack Intelligent Master Plugin"
3. 重新提问

### 问题5：Docker内无法访问localhost

**原因**：Docker网络隔离

**解决**：
- ✅ 正确：`host.docker.internal:8011`
- ❌ 错误：`localhost:8011`
- ❌ 错误：`127.0.0.1:8011`

---

## 🔍 验证安装成功的方法

### 方法1：查看Plugin列表

在Functions页面应该能看到：
```
✅ AI Stack Intelligent Master Plugin
   🟢 已启用
```

### 方法2：查看OpenWebUI日志

```bash
docker logs open-webui -f
```

成功加载Plugin时会有日志输出。

### 方法3：测试对话

在聊天框输入测试问题，观察回答中是否有：
- "🧠 AI Stack智能分析中..."
- 【RAG知识】、【系统数据】、【专家建议】等标记

### 方法4：检查学习系统

```bash
# 提问几次后，查看学习记录
curl http://localhost:8019/api/learning/stats

# 应该返回
{
  "total_learning_samples": 3,
  "intelligence_level": "初级 (Lv.1)",
  "status": "系统持续学习进化中"
}
```

---

## 📊 完整的工作流程

```
1. 用户在OpenWebUI提问
   ↓
2. Plugin拦截处理（inlet函数）
   ├─ 智能识别问题类型
   ├─ RAG检索相关知识
   ├─ 调用对应系统API（ERP/股票/趋势等）
   └─ 专家系统分析
   ↓
3. 组合增强的上下文
   ↓
4. 传递给AI模型
   ↓
5. AI生成专业回答
   ↓
6. 返回给用户（outlet函数）
   ├─ 对话自动入库RAG
   ├─ 提交学习系统
   └─ 触发进化检查
   ↓
7. 系统越来越聪明 ✨
```

---

## 🎯 核心要点

### ✅ 必须做的
1. **使用管理员账号**（第一个注册的账号）
2. **API地址用 `host.docker.internal`**（不是localhost）
3. **启用所有6个功能开关**
4. **在聊天中勾选Function**
5. **确保AI Stack服务运行中**

### ❌ 常见错误
1. ❌ 使用 `localhost` 而不是 `host.docker.internal`
2. ❌ 忘记启用功能开关
3. ❌ 忘记在聊天中勾选Function
4. ❌ Plugin主开关是灰色（未启用）
5. ❌ AI Stack服务未运行

---

## 💡 备选方案

如果OpenWebUI集成遇到困难，可以使用：

### 方案1：统一中控台
访问：**http://localhost:8000**
- 可视化界面
- 点击访问各系统
- 立即可用

### 方案2：API Gateway
```bash
# RAG搜索
curl "http://localhost:9000/gateway/rag/search?query=机器学习"

# ERP查询
curl "http://localhost:9000/gateway/erp/financial"

# 股票查询
curl "http://localhost:9000/gateway/stock/price/600519"
```

### 方案3：直接访问各系统
- RAG: http://localhost:8011
- ERP: http://localhost:8013/dashboard
- 股票: http://localhost:8014/dashboard
- OpenWebUI: http://localhost:3000

---

## 🎊 安装完成后的效果

### 智能对话示例

**你**：分析一下茅台的投资价值

**AI（集成AI Stack）**：
```
🧠 AI Stack智能分析中...
✅ 已集成实时股票数据+知识库+专家分析

【📊 实时股票数据】
贵州茅台 (600519)
当前价格: ¥1,850.00
涨跌幅: +2.5%
成交量: 1,234,567手

【📚 知识库检索】
1. 贵州茅台是中国高端白酒龙头企业...
2. 历史表现：过去5年年化收益率25%...

【👨‍🔬 投资专家分析】
💡 基本面分析：
   - 护城河深厚，品牌价值极高
   - 现金流充沛，ROE持续>20%
   
⚠️  风险提示：
   - 估值偏高，PE超过40倍
   - 需关注政策风险和消费环境变化
   
📈 建议：
   - 长期投资者可分批建仓
   - 短期波动较大，建议设置止损
   - 建议配置比例不超过投资组合的15%

基于以上数据和专家分析，贵州茅台具有较好的长期投资价值...
```

**系统后台自动**：
- 📚 本次对话已入库RAG
- 🧠 提交学习系统（第N次交互）
- 📊 更新知识图谱

---

## 📋 快速命令参考

### 复制Plugin到剪贴板
```bash
cd /Users/ywc/ai-stack-super-enhanced/openwebui-plugin
cat ai_stack_intelligent_plugin.py | pbcopy
```

### 检查服务状态
```bash
# OpenWebUI
docker ps | grep open-webui

# AI Stack
curl http://localhost:9000/gateway/status
```

### 重启OpenWebUI
```bash
docker restart open-webui
```

### 查看OpenWebUI日志
```bash
docker logs open-webui -f
```

### 启动AI Stack服务
```bash
cd /Users/ywc/ai-stack-super-enhanced
./scripts/start_all_final.sh
```

---

## 🎊 总结

安装步骤：
1. ✅ 打开OpenWebUI (http://localhost:3000)
2. ✅ Settings → Workspace → Functions
3. ✅ 点击 "+" → 粘贴代码 → Save
4. ✅ 配置API地址（host.docker.internal）
5. ✅ 启用所有6个功能开关
6. ✅ 确保Plugin主开关是绿色
7. ✅ 在聊天中勾选Function
8. ✅ 开始智能对话！

关键点：
- 🔑 使用 `host.docker.internal` 不是 `localhost`
- 🔑 必须是管理员账号
- 🔑 所有开关都要启用
- 🔑 在聊天中勾选Function

**Plugin已在剪贴板，现在就可以安装了！** 🚀


