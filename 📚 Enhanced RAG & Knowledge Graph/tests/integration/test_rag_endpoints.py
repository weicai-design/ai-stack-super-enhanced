"""
Placeholder integration tests for the RAG endpoints.

The previous version accidentally contained directory listings instead of
executable tests, which caused pytest to fail during collection. Replace this
file with real tests when the RAG endpoints are ready to be exercised.
"""

import pytest


@pytest.mark.skip(reason="Integration tests for RAG endpoints not yet implemented.")
def test_rag_endpoints_placeholder():
    """Placeholder to avoid pytest collection errors."""
    assert True