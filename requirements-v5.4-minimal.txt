# AI-STACK V5.4 最小依赖清单 - 核心功能可用
# 安装: pip install -r requirements-v5.4-minimal.txt

# 核心框架（必须）
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
httpx==0.25.0

# 数据库（必须）
sqlalchemy==2.0.23

# RAG核心（必须）
numpy==1.26.0
sentence-transformers==2.2.2

# 基础工具（必须）
psutil==5.9.6
python-dotenv==1.0.0

# LLM（二选一）
# openai==1.3.5  # 如使用OpenAI GPT-4
# 或启动Ollama本地服务（推荐）

# 其他可选依赖可根据需要安装
# 语音: openai-whisper, gtts
# 文件生成: python-docx, openpyxl, reportlab
# 翻译搜索: googletrans, duckduckgo-search


