# Web 框架
fastapi==0.121.1
uvicorn[standard]==0.38.0
python-multipart==0.0.20
httpx==0.28.1
pydantic==2.12.4

# AI / ML 核心依赖
numpy>=2.1.3
torch>=1.11.0  # PyTorch 是 sentence-transformers 的必需依赖，需在 sentence-transformers 之前安装
sentence-transformers>=3.0.0

# 文档处理
pymupdf>=1.24.0
pdfplumber>=0.11.0
python-docx>=1.1.0

# 向量数据库
faiss-cpu>=1.7.4

# 监控与工具
prometheus-client>=0.20.0
pytest==8.3.3
PyYAML==6.0.2
psutil>=7.1.0
packaging>=21.0  # 用于版本比较（check_dependencies.py 需要）

# 安全与加密
cryptography>=43.0.0

# 环境配置
python-dotenv>=1.0.0  # 用于加载 .env 文件