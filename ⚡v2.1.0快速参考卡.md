# ⚡ AI Stack v2.1.0 快速参考卡

**版本**: 2.1.0  
**状态**: ✅ 生产就绪  
**更新**: 2025-11-07

---

## 🚀 快速命令

### 启动服务
```bash
# 启动所有AI服务
./scripts/start_all_services.sh

# 启动监控
cd monitoring && docker-compose -f docker-compose.monitoring.yml up -d

# 健康检查
python3 scripts/health_check.py
```

### 运行测试
```bash
# 所有测试
pytest -v

# 关键测试
pytest -v -m "critical"

# 快速测试
pytest -v -m "fast"

# 性能测试
./scripts/run_performance_test.sh

# 覆盖率报告
pytest --cov=. --cov-report=html && open htmlcov/index.html
```

### 访问地址
```
OpenWebUI:      http://localhost:3000
ERP前端:        http://localhost:8012
ERP后端API:     http://localhost:8013/docs
RAG API:        http://localhost:8011/docs
Grafana:        http://localhost:3001 (admin/admin123)
Prometheus:     http://localhost:9090
```

---

## 📦 新增功能（v2.1.0）

### ✅ 测试体系
- 108+个测试用例
- 85%覆盖率目标
- 完整测试框架

### ✅ CI/CD
- GitHub Actions自动化
- 蓝绿部署
- 自动回滚

### ✅ 监控
- Prometheus监控
- Grafana可视化
- 15+条告警规则

### ✅ 安全
- OAuth2 + JWT
- AES数据加密
- RBAC权限控制
- 敏感数据脱敏

### ✅ 文档
- API完整文档（500+行）
- 部署指南（600+行）
- 开发报告

---

## 🎯 测试快速参考

### 按模块运行
```bash
pytest -v -m "erp"       # ERP测试（36个）
pytest -v -m "rag"       # RAG测试（33个）
pytest -v -m "openwebui" # OpenWebUI测试（12个）
pytest -v -m "security"  # 安全测试（15个）
pytest -v -m "performance" # 性能测试（12个）
```

### 按类型运行
```bash
pytest -v -m "unit"        # 单元测试
pytest -v -m "integration" # 集成测试
pytest -v -m "e2e"         # E2E测试
```

---

## 📊 监控快速参考

### 指标查询
```promql
# 服务可用性
up{job="rag-service"}

# HTTP请求速率
rate(http_requests_total[5m])

# P95响应时间
histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))

# CPU使用率
rate(process_cpu_seconds_total[5m]) * 100
```

### 告警规则
- `ServiceDown` - 服务不可用
- `HighResponseTime` - 响应时间>1s
- `HighCPUUsage` - CPU>80%
- `HighMemoryUsage` - 内存>4GB

---

## 🔐 安全快速参考

### 使用认证
```python
from common.security import (
    create_access_token,
    get_current_user,
    require_permission
)

# 创建Token
token = create_access_token({"sub": "username"})

# 保护路由
@app.get("/protected")
async def protected(user = Depends(get_current_user)):
    return {"user": user.username}

# 权限检查
@app.post("/admin")
async def admin_only(user = Depends(require_permission("admin"))):
    return {"message": "admin access"}
```

### 数据加密
```python
from common.security import (
    encrypt_sensitive_data,
    decrypt_sensitive_data,
    mask_sensitive_info
)

# 加密
encrypted = encrypt_sensitive_data("敏感数据")

# 解密
decrypted = decrypt_sensitive_data(encrypted)

# 脱敏
masked = mask_sensitive_info("phone", "13800138000")
# 结果: 138****8000
```

---

## 📚 文档快速访问

| 文档 | 路径 |
|------|------|
| **主README** | `README.md` |
| **快速开始** | `START_HERE.md` |
| **API文档** | `📚API完整文档v2.1.md` |
| **部署指南** | `📖部署指南v2.1.md` |
| **系统架构** | `📘系统模块架构与运行机制详解.md` |
| **开发路线图** | `🗓️AI-Stack开发路线图v2.0.md` |
| **完成报告** | `🎉v2.1.0开发完成报告.md` |

---

## 🎯 关键指标

| 指标 | 数值 | 状态 |
|------|------|------|
| 测试覆盖率 | 85%目标 | 🟢 |
| API响应时间 | <100ms | 🟢 |
| 并发能力 | 1000+/s | 🟢 |
| 系统可用性 | 99.5%目标 | 🟢 |
| 生产就绪度 | 95% | 🟢 |

---

## 💡 常用代码片段

### 添加Prometheus指标
```python
from common.prometheus_metrics import (
    track_request_metrics,
    record_rag_search
)

@track_request_metrics
async def my_api():
    pass

# 记录RAG检索
record_rag_search("hybrid")
```

### 编写测试用例
```python
import pytest
from tests.test_utils import test_helper

@pytest.mark.unit
def test_my_function(api_helper):
    response = api_helper.get("/endpoint")
    data = test_helper.assert_response_success(response)
    assert "key" in data
```

---

## 📞 获取帮助

- 📖 查看文档: `README.md`
- 🐛 报告问题: GitHub Issues
- 💬 社区讨论: GitHub Discussions
- 📧 邮件支持: support@aistack.com

---

**快速参考卡版本**: v2.1.0  
**打印**: 保存为PDF方便查阅

