# 快速启动指南

## ✅ 第1步：配置环境变量

### 1.1 复制环境变量文件

**命令：**
```bash
cp env.example .env
```

**说明：** 这行命令后面不要跟注释，直接执行即可。

---

### 1.2 编辑 .env 文件

**方法1：使用 nano 编辑器（推荐，简单）**

```bash
nano .env
```

**操作说明：**
- 按 `Ctrl + X` 退出
- 按 `Y` 保存
- 按 `Enter` 确认

**方法2：使用 vim 编辑器**

```bash
vim .env
```

**操作说明：**
- 按 `i` 进入编辑模式
- 编辑完成后按 `Esc`
- 输入 `:wq` 保存并退出

**方法3：使用其他编辑器**

```bash
# 使用 VS Code（如果已安装）
code .env

# 使用系统默认文本编辑器
open -a TextEdit .env  # macOS
```

---

### 1.3 设置 JWT 密钥

在 `.env` 文件中找到这一行：
```bash
JWT_SECRET_KEY=your-super-secret-jwt-key
```

**替换为安全的随机字符串：**

```bash
# 方法1：使用 openssl 生成（推荐）
openssl rand -hex 32

# 复制生成的结果，然后编辑 .env 文件
JWT_SECRET_KEY=这里粘贴生成的密钥
```

**或者使用 Python 生成：**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## ✅ 第2步：打开权限管理页面

### 方法1：直接在浏览器中打开（推荐）

**macOS：**
```bash
open "📚 Enhanced RAG & Knowledge Graph/web/permission_management.html"
```

**或者：**
```bash
cd "📚 Enhanced RAG & Knowledge Graph/web"
open permission_management.html
```

**Linux：**
```bash
xdg-open "📚 Enhanced RAG & Knowledge Graph/web/permission_management.html"
```

**Windows：**
```bash
start "📚 Enhanced RAG & Knowledge Graph/web/permission_management.html"
```

---

### 方法2：手动打开文件

1. 在 Finder（macOS）或文件管理器中找到：
   ```
   ai-stack-super-enhanced/📚 Enhanced RAG & Knowledge Graph/web/permission_management.html
   ```

2. 双击文件，会在默认浏览器中打开

---

### 方法3：通过后端服务访问（如果已启动）

如果后端服务正在运行，可以通过：
```
http://localhost:8000/web/permission_management.html
```

---

## ✅ 第3步：检查配置

### 3.1 验证 .env 文件已创建

```bash
ls -la .env
```

**应该看到：**
```
-rw-r--r--  1 ywc  staff  1234  Jan XX XX:XX .env
```

---

### 3.2 检查 SQLite 数据库

```bash
# 检查数据库文件是否存在（创建 API Key 后会自动生成）
ls -la data/api_keys.db
```

---

## 🚀 快速命令汇总

### 一键设置（复制粘贴运行）

```bash
# 1. 复制环境变量文件
cp env.example .env

# 2. 生成 JWT 密钥（复制输出结果）
python3 -c "import secrets; print('JWT_SECRET_KEY=' + secrets.token_urlsafe(32))"

# 3. 编辑 .env 文件，将上面生成的密钥粘贴进去
nano .env

# 4. 打开权限管理页面
open "📚 Enhanced RAG & Knowledge Graph/web/permission_management.html"
```

---

## 📝 常见问题

### Q: 编辑文件时找不到 JWT_SECRET_KEY？

**A:** 在 `.env` 文件中搜索 `JWT_SECRET`，找到后修改值。

---

### Q: 如何查看 .env 文件内容？

**A:** 使用以下命令：
```bash
cat .env
```

或者只查看 JWT 相关配置：
```bash
grep JWT .env
```

---

### Q: 权限管理页面打不开？

**A:** 
1. 确认文件路径正确
2. 尝试使用完整路径：
   ```bash
   open "/Users/ywc/ai-stack-super-enhanced/📚 Enhanced RAG & Knowledge Graph/web/permission_management.html"
   ```
3. 或者进入文件所在目录后打开：
   ```bash
   cd "/Users/ywc/ai-stack-super-enhanced/📚 Enhanced RAG & Knowledge Graph/web"
   open permission_management.html
   ```

---

## 💡 提示

1. **注释符号 `#`**：在终端中，`#` 后面的内容会被忽略，所以看到 "command not found: #" 是正常的。

2. **文件路径**：如果路径包含空格或特殊字符，请用引号括起来：
   ```bash
   "📚 Enhanced RAG & Knowledge Graph/web/permission_management.html"
   ```

3. **相对路径和绝对路径**：
   - 相对路径：`web/permission_management.html`（需要先进入正确目录）
   - 绝对路径：`/Users/ywc/ai-stack-super-enhanced/📚 Enhanced RAG & Knowledge Graph/web/permission_management.html`

---

**按照上述步骤操作即可！** 🚀



