[pytest]
norecursedirs =
    tests/performance
    .git
    .backup
    node_modules
    */node_modules
    */.git
    */.backup

pythonpath =
    .
    super_agent_main_interface

addopts =
    --ignore=.backup
    --ignore=.git
    --ignore=.venv
    --ignore=tests/performance/test_performance_suite.py
    --rootdir=super_agent_main_interface

