# 📖 AI Stack v2.3.0 使用指南

**版本**: v2.3.0 - 业务功能增强版  
**更新时间**: 2025-11-07  
**状态**: ✅ 核心功能完成

---

## 🎯 新功能概览

v2.3.0 专注于业务功能深化和AI能力增强，引入了多币种、税务管理、预算控制、多模型AI支持等企业级功能。

**核心亮点**:
- 💰 多币种国际化支持
- 📊 税务管理系统
- 📋 预算控制体系
- 💼 成本精细核算
- 🤖 GPT-4 + Claude多模型
- 🎯 智能模型路由
- 💬 智能问答系统
- 🎨 Prompt工程化

---

## 💰 多币种功能

### 快速开始

```python
from finance.multi_currency import get_currency_manager

# 获取管理器
manager = get_currency_manager()

# 1. 查看支持的货币
currencies = manager.list_currencies()
print(f"支持 {len(currencies)} 种货币")

# 2. 货币转换
amount_cny = manager.convert(
    amount=Decimal("100"),
    from_currency="USD",
    to_currency="CNY"
)
print(f"$100 = ¥{amount_cny}")

# 3. 格式化显示
formatted = manager.format_amount(amount_cny, "CNY")
print(f"格式化: {formatted}")

# 4. 多币种总额
amounts = [
    {"amount": 100, "currency": "USD"},
    {"amount": 500, "currency": "CNY"},
    {"amount": 80, "currency": "EUR"}
]
total = manager.get_multi_currency_total(amounts, target_currency="CNY")
print(f"人民币总额: ¥{total['CNY']}")
```

### 支持的货币

| 货币 | 代码 | 符号 | 小数位 |
|------|------|------|--------|
| 人民币 | CNY | ¥ | 2 |
| 美元 | USD | $ | 2 |
| 欧元 | EUR | € | 2 |
| 英镑 | GBP | £ | 2 |
| 日元 | JPY | ¥ | 0 |
| 港币 | HKD | HK$ | 2 |
| 韩元 | KRW | ₩ | 0 |

### 汇率更新

```python
# 手动设置汇率
manager.set_exchange_rate("USD", "CNY", Decimal("7.2"))

# 从API自动获取
rate = manager.get_exchange_rate("USD", "CNY")

# 批量更新所有汇率
manager.update_all_rates_from_api()
```

---

## 📊 税务管理

### 计算税额

```python
from finance.tax_manager import get_tax_manager, TaxType

manager = get_tax_manager()

# 计算增值税（不含税金额）
tax_calc = manager.calculate_tax(
    amount=Decimal("10000"),
    tax_type=TaxType.VAT,
    is_tax_included=False
)

print(f"金额: ¥{tax_calc.amount}")
print(f"税率: {tax_calc.tax_rate}%")
print(f"税额: ¥{tax_calc.tax_amount}")
print(f"价税合计: ¥{tax_calc.total_amount}")
```

### 发票管理

```python
# 创建发票
invoice = manager.create_invoice(
    invoice_number="INV20251107001",
    seller_name="ABC公司",
    seller_tax_id="123456789",
    buyer_name="XYZ公司",
    buyer_tax_id="987654321",
    amount=Decimal("10000"),
    tax_calculation=tax_calc
)

print(f"发票号: {invoice.invoice_number}")
print(f"含税总额: ¥{invoice.total_amount}")
```

### 税务报表

```python
# 生成月度税务报表
report = manager.get_tax_report(
    start_date=date(2025, 11, 1),
    end_date=date(2025, 11, 30)
)

print(f"发票数量: {report['summary']['invoice_count']}")
print(f"总税额: ¥{report['summary']['total_tax']}")
```

---

## 📋 预算管理

### 创建预算

```python
from finance.budget_manager import get_budget_manager, Budget, BudgetCategory

manager = get_budget_manager()

# 创建年度预算
budget = Budget(
    name="2025年度营销预算",
    category=BudgetCategory.EXPENSE,
    period=BudgetPeriod.ANNUAL,
    year=2025,
    department="市场部",
    amount=Decimal("1000000"),
    description="年度营销推广预算"
)

budget = manager.create_budget(budget)
```

### 预算执行

```python
# 记录预算执行
execution = BudgetExecution(
    budget_id=budget.id,
    actual_amount=Decimal("100000"),
    execution_date=date.today(),
    description="Q1营销活动支出"
)

manager.record_execution(execution)
```

### 预算分析

```python
# 分析预算执行情况
analysis = manager.analyze_budget(budget.id)

print(f"预算金额: ¥{analysis.budget_amount}")
print(f"实际支出: ¥{analysis.actual_amount}")
print(f"执行率: {analysis.execution_rate}%")
print(f"剩余预算: ¥{analysis.variance}")
print(f"状态: {analysis.status}")  # under/match/over
```

---

## 💼 成本核算

### 成本中心管理

```python
from finance.cost_accounting import get_cost_manager, CostCenter

manager = get_cost_manager()

# 创建成本中心
center = CostCenter(
    code="CC001",
    name="生产车间一",
    department="生产部",
    manager="张三"
)

center = manager.create_cost_center(center)
```

### 记录成本

```python
from finance.cost_accounting import CostItem, CostType

# 记录直接材料成本
cost = CostItem(
    cost_center_id=center.id,
    cost_type=CostType.DIRECT_MATERIAL,
    item_name="原材料采购",
    quantity=Decimal("1000"),
    unit_cost=Decimal("50"),
    date=date.today()
)

manager.record_cost(cost)
```

### 成本分摊

```python
# 将辅助车间成本分摊到生产车间
manager.allocate_cost(
    source_center=1,  # 辅助车间
    target_centers=[2, 3, 4],  # 三个生产车间
    amount=Decimal("30000"),
    method="equal"  # 平均分摊
)
```

---

## 🤖 多模型AI使用

### GPT-4集成

```python
from ai.models.gpt4_client import get_gpt4_client

# 获取GPT-4客户端
client = get_gpt4_client(model="gpt-4-turbo-preview")

# 聊天对话
messages = [
    {"role": "system", "content": "你是一个专业的AI助手"},
    {"role": "user", "content": "什么是机器学习？"}
]

response = await client.chat_completion(messages)
print(f"GPT-4: {response['content']}")
print(f"Token: {response['usage']['total_tokens']}")
```

### Claude集成

```python
from ai.models.claude_client import get_claude_client

# 获取Claude客户端（适合长文本）
client = get_claude_client(model="claude-3-sonnet-20240229")

# 文档分析
analysis = await client.analyze_document(
    document_text=long_document,
    task="summarize"
)

print(f"摘要: {analysis['result']}")
```

### 智能模型路由

```python
from ai.models.model_router import get_model_router, RoutingStrategy

router = get_model_router()

# 自动选择最佳模型
model = router.select_model(
    task="生成技术文档",
    strategy=RoutingStrategy.QUALITY_OPTIMIZED,
    min_quality=0.9
)

print(f"选择的模型: {model.name}")
print(f"成本: ${model.cost_per_1k_output}/1K tokens")

# 获取推荐
recommendations = router.get_model_recommendations(
    task_description="分析一份长文档并生成摘要"
)

for rec in recommendations:
    print(f"{rec['model'].name}: {', '.join(rec['reasons'])}")
```

---

## 💬 智能问答系统

### 多轮对话

```python
from qa.intelligent_qa import get_qa_system

qa = get_qa_system()

# 创建对话会话
conv_id = "user_123_session"

# 第一轮问答
response1 = await qa.answer_question(
    conversation_id=conv_id,
    query="什么是人工智能？"
)
print(f"A: {response1.answer}")

# 第二轮问答（自动使用上下文）
response2 = await qa.answer_question(
    conversation_id=conv_id,
    query="它有哪些应用场景？"  # "它"会基于上下文理解
)
print(f"A: {response2.answer}")
print(f"置信度: {response2.confidence}")
print(f"使用的上下文: {response2.context_used}")
```

### 对话管理

```python
# 查看对话摘要
summary = qa.get_conversation_summary(conv_id)
print(f"消息数: {summary['message_count']}")
print(f"持续时间: {summary['duration_minutes']}分钟")
```

---

## 🎨 Prompt工程

### 使用Prompt模板

```python
from ai.prompt_engineering.prompt_template_manager import get_template_manager

manager = get_template_manager()

# 1. 列出可用模板
templates = manager.list_templates()
for tmpl in templates:
    print(f"{tmpl.name} ({tmpl.category})")

# 2. 使用模板生成Prompt
prompt = manager.render_template(
    template_id=1,  # 文档摘要模板
    variables={
        "document": "这是一篇很长的文档...",
        "word_limit": "200"
    }
)

print(f"生成的Prompt:\n{prompt}")

# 3. 创建自定义模板
from ai.prompt_engineering.prompt_template_manager import PromptTemplate, PromptCategory

custom = PromptTemplate(
    name="数据分析助手",
    category=PromptCategory.ANALYSIS,
    template="分析数据：{data}\n维度：{dimensions}",
    variables=["data", "dimensions"],
    author="user"
)

manager.create_template(custom)
```

### Prompt优化

```python
from ai.prompt_engineering.prompt_optimizer import get_optimizer

optimizer = get_optimizer()

# 优化Prompt
original = "帮我分析这个数据"

optimized = await optimizer.optimize_prompt(
    original_prompt=original,
    objective="clarity"
)

print(f"原始: {original}")
print(f"优化后: {optimized}")

# A/B测试
results = await optimizer.ab_test(
    prompt_a="原始版本",
    prompt_b="优化版本",
    test_cases=["测试1", "测试2", "测试3"]
)

print(f"获胜者: {results['winner']}")
print(f"改进幅度: {results['improvement']*100}%")
```

---

## 🛎️ 智能客服

### 处理客户查询

```python
from ai.applications.smart_customer_service import get_customer_service

service = get_customer_service()

# 处理客户问题
result = await service.handle_query(
    customer_id=123,
    query="你们的产品价格是多少？"
)

if result["can_auto_respond"]:
    # 自动回答
    print(f"自动回答: {result['answer']}")
else:
    # 创建工单
    print(f"工单已创建: #{result['ticket_id']}")
    print(f"消息: {result['message']}")
```

### 工单统计

```python
# 查看工单统计
stats = service.get_ticket_stats()

print(f"总工单数: {stats['total_tickets']}")
print(f"按状态统计: {stats['by_status']}")
print(f"按优先级统计: {stats['by_priority']}")
```

---

## 📄 文档摘要

### 生成摘要

```python
from ai.applications.document_summarizer import get_summarizer, SummaryType

summarizer = get_summarizer()

# 生成简要摘要
summary = await summarizer.summarize(
    document_id="doc_001",
    document_text=long_document,
    summary_type=SummaryType.BRIEF,
    max_length=200
)

print(f"摘要: {summary.summary}")
print(f"关键点: {summary.key_points}")
print(f"关键词: {summary.keywords}")
print(f"置信度: {summary.confidence}")
```

### 多文档聚合

```python
# 聚合多个文档的摘要
aggregated = await summarizer.aggregate_summaries(
    document_ids=["doc_001", "doc_002", "doc_003"]
)

print(f"聚合摘要:\n{aggregated}")
```

---

## 📊 供应商评分

### 评估供应商

```python
from supply_chain.supplier_scoring import get_scoring_system, SupplierPerformance

system = get_scoring_system()

# 准备绩效数据
performance = SupplierPerformance(
    supplier_id=1,
    period_start=date(2025, 1, 1),
    period_end=date(2025, 10, 31),
    total_orders=100,
    defect_orders=3,
    defect_rate=Decimal("3.0"),
    on_time_deliveries=92,
    on_time_rate=Decimal("92"),
    avg_delay_days=Decimal("1.5"),
    price_variance=Decimal("3.5"),
    payment_terms=30,
    response_time_hours=Decimal("4"),
    issue_resolution_rate=Decimal("95")
)

# 评估供应商
score_card = system.evaluate_supplier(
    supplier_id=1,
    supplier_name="优质供应商A",
    performance=performance
)

print(f"综合评分: {score_card.total_score}分")
print(f"评级: {score_card.grade}级")
print(f"质量: {score_card.quality_score}")
print(f"交付: {score_card.delivery_score}")
print(f"价格: {score_card.price_score}")
```

### 查看排名

```python
# 获取前10名供应商
top_suppliers = system.get_supplier_rank(10)

for i, card in enumerate(top_suppliers, 1):
    print(f"{i}. {card.supplier_name} - {card.grade}级 ({card.total_score}分)")
```

---

## 🔍 多模态检索

### 以图搜图

```python
from multimodal.multimodal_retrieval import get_multimodal_retrieval

retrieval = get_multimodal_retrieval()

# 搜索相似图片
results = await retrieval.search_by_image(
    image_path="/path/to/query.jpg",
    top_k=10
)

print(f"找到 {results.total} 个相似图片")
for item in results.items:
    print(f"- {item['file_path']} (相似度: {item['similarity']})")
```

### 文本搜索音频

```python
# 根据文本内容搜索音频
results = await retrieval.search_audio_by_text(
    query_text="关于人工智能的讨论",
    top_k=5
)

for item in results.items:
    print(f"- {item['file_path']}")
    print(f"  转录文本: {item['transcript']}")
```

### 视频内容搜索

```python
# 根据描述搜索视频
results = await retrieval.search_video_by_description(
    description="产品演示和使用教程",
    top_k=5
)

for item in results.items:
    print(f"- {item['file_path']}")
    print(f"  时长: {item['duration_seconds']}秒")
```

---

## 📈 FastAPI集成示例

### 多币种API

```python
from fastapi import FastAPI, HTTPException
from finance.multi_currency import get_currency_manager

app = FastAPI()

@app.post("/api/currency/convert")
async def convert_currency(
    amount: float,
    from_currency: str,
    to_currency: str
):
    manager = get_currency_manager()
    
    converted = manager.convert(
        Decimal(str(amount)),
        from_currency,
        to_currency
    )
    
    if converted is None:
        raise HTTPException(status_code=400, detail="无法获取汇率")
    
    return {
        "amount": amount,
        "from_currency": from_currency,
        "to_currency": to_currency,
        "converted_amount": float(converted),
        "formatted": manager.format_amount(converted, to_currency)
    }
```

### 智能客服API

```python
@app.post("/api/customer-service/query")
async def handle_customer_query(
    customer_id: int,
    query: str
):
    service = get_customer_service()
    
    result = await service.handle_query(customer_id, query)
    
    return result
```

### 智能问答API

```python
@app.post("/api/qa/ask")
async def ask_question(
    session_id: str,
    question: str
):
    qa = get_qa_system()
    
    response = await qa.answer_question(
        conversation_id=session_id,
        query=question
    )
    
    return {
        "answer": response.answer,
        "confidence": response.confidence,
        "sources": response.sources,
        "context_used": response.context_used
    }
```

---

## 🔧 配置说明

### 环境变量

添加以下环境变量到`.env`文件：

```bash
# AI模型配置
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# 汇率API（可选）
EXCHANGE_RATE_API_KEY=your_api_key

# 其他配置保持不变
```

### 依赖安装

```bash
# 安装v2.3新增依赖
pip install anthropic==0.7.0
pip install openai==1.3.0

# 或安装完整依赖
pip install -r requirements.txt
```

---

## 📊 性能提升

### v2.3.0 vs v2.2.0

| 功能 | v2.2 | v2.3 | 改进 |
|------|------|------|------|
| AI模型 | 单一 | 多模型 | ⭐ 灵活性 |
| 模型选择 | 固定 | 智能路由 | ⭐ 成本优化 |
| 财务功能 | 基础 | 国际化 | ⭐ 业务价值 |
| 问答能力 | 简单 | 上下文理解 | ⭐ 用户体验 |

---

## 🎯 最佳实践

### 1. 模型选择建议

```python
# ✅ 推荐做法
# 简单任务用便宜的模型
simple_model = router.select_model(
    task="简单问答",
    strategy=RoutingStrategy.COST_OPTIMIZED
)

# 复杂任务用高质量模型
complex_model = router.select_model(
    task="专业分析",
    strategy=RoutingStrategy.QUALITY_OPTIMIZED
)

# ❌ 避免
# 不要用GPT-4做简单任务（成本高）
# 不要用免费模型做关键任务（质量低）
```

### 2. 多币种使用建议

```python
# ✅ 推荐做法
# 定期更新汇率
manager.update_all_rates_from_api()

# 使用基础货币统一核算
total = manager.get_multi_currency_total(amounts, "CNY")

# ❌ 避免
# 不要使用过期汇率
# 不要混合不同币种直接相加
```

---

## 🐛 故障排查

### GPT-4/Claude连接失败

```bash
# 检查API密钥
echo $OPENAI_API_KEY
echo $ANTHROPIC_API_KEY

# 测试连接
python -c "from ai.models.gpt4_client import get_gpt4_client; client = get_gpt4_client(); print('连接成功')"
```

### 汇率获取失败

```bash
# 检查网络连接
curl https://api.exchangerate-api.com/v4/latest/USD

# 手动设置汇率
python -c "from finance.multi_currency import get_currency_manager; m = get_currency_manager(); m.set_exchange_rate('USD', 'CNY', Decimal('7.2'))"
```

---

## 📚 相关文档

- [v2.3.0完成报告](🎉v2.3.0开发完成报告.md)
- [完整开发总结](🌈AI-Stack完整开发总结-v2.3.0.md)
- [v2.2.0使用指南](📖v2.2.0使用指南.md)
- [API文档](docs/API_DOCUMENTATION_v2.1.md)

---

## 🎊 功能对比

### v2.3新增功能一览

| 模块 | 新增功能 | 价值 |
|------|---------|------|
| ERP财务 | 多币种/税务/预算/成本 | ⭐⭐⭐⭐⭐ |
| ERP供应链 | 供应商评分系统 | ⭐⭐⭐⭐ |
| AI模型 | GPT-4/Claude集成 | ⭐⭐⭐⭐⭐ |
| AI路由 | 智能模型选择 | ⭐⭐⭐⭐⭐ |
| RAG | 智能问答/多模态检索 | ⭐⭐⭐⭐⭐ |
| 应用场景 | 智能客服/文档摘要 | ⭐⭐⭐⭐⭐ |
| Prompt | 工程化管理 | ⭐⭐⭐⭐ |

---

<div align="center">

# 🚀 开始使用 v2.3.0

**业务功能增强 • AI能力升级 • 多模型支持**

[开发总结](🌈AI-Stack完整开发总结-v2.3.0.md) • [完成报告](🎉v2.3.0开发完成报告.md) • [API文档](docs/API_DOCUMENTATION_v2.1.md)

---

**Made with ❤️ by AI Stack Team**

*v2.3.0 - Business Enhanced Edition*

</div>


















